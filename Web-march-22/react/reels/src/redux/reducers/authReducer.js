const initalAuth = {
    user: null,
    loggeIn: false
}
function authReducer(state = initalAuth, action) {
    switch (action.type) {
        default:
            return state;
    }
}
export default authReducer;