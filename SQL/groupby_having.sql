-- Where : will filter on row level (to individual row)
-- having : will filter on group level (aggregate function)
Group by x
group by x,y-> jinka x and y dono hi same ho wo 
group by x,y,z -> jinka x,y ,z values saari hi same ho 
Select count(Id) ,country from customer 
where country <> 'USA'
GROUP BY country
Having Count (Id)>=9


-- Having without group by
select sum(Salary) from ibs having max(Salary)

-- Q Write down the query to fetch Project name 
-- assign to more than one Employee

JOins are used to merge two tables on the basis of some key 
by using som join you can retreive data from  two or more tables
